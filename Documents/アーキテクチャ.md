# アーキテクチャ — EncodeLab

最終更新: 2026-04-29

## 1. プロセス構成

Electron 標準の 3 プロセス構成。

```text
┌─────────────────────────────────────────────────────────────┐
│ main process (Node.js)                                      │
│  - Window 制御 / electron-updater / IPC ハンドラ            │
│  - ファイル I/O (dialog + fs)                               │
│  - MIME 自動判定 (file-type ESM, 動的 import)               │
│  - ハッシュ計算 (Node.js crypto)                            │
└─────────────────────────────────────────────────────────────┘
              ▲           ▲
              │ IPC       │ contextBridge
              ▼           │
┌─────────────────────────────────────────────────────────────┐
│ preload (sandboxed)                                         │
│  - window.encodelab に IPC API を expose                    │
└─────────────────────────────────────────────────────────────┘
              ▲
              │
┌─────────────────────────────────────────────────────────────┐
│ renderer (React + MUI)                                      │
│  - 変換エンジン (CanonicalValue ベース)                     │
│  - 各エンコーダ / デコーダ (自前実装)                       │
│  - QR 生成 (qrcode) / QR 読取 (jsqr)                        │
│  - バーコード生成 (bwip-js) / 読取 (@zxing/library)         │
│  - UI (3 カラム / Pane / 中央コントロール)                  │
│  - Zustand ストア                                           │
└─────────────────────────────────────────────────────────────┘
```

## 2. ディレクトリ構成

```text
src/
├── main/                       # Electron main プロセス
│   ├── index.ts                # 起動・ウィンドウ生成
│   ├── ipc/
│   │   ├── index.ts            # IPC ハンドラ登録エントリ
│   │   ├── updater.ts          # 自動アップデート IPC
│   │   └── conversion.ts       # ファイル/MIME/ハッシュ IPC
│   ├── services/
│   │   ├── updater.ts
│   │   ├── file.ts             # ファイル選択 / 保存
│   │   ├── mime.ts             # file-type ラッパ
│   │   └── hash.ts             # crypto ラッパ
│   └── utils/console-bridge.ts
│
├── preload/index.ts            # contextBridge で window.encodelab を expose
│
├── shared/
│   ├── types.ts                # IPC 型定義
│   ├── constants.ts            # IPC チャンネル名
│   ├── ipc.ts                  # IpcApi 型
│   └── conversion/
│       ├── catalog.ts          # カテゴリ・タイプ定義
│       ├── compatibility.ts    # 変換可否判定
│       └── domain.ts           # CanonicalValue 型
│
└── renderer/                   # React + MUI
    ├── main.tsx                # エントリ
    ├── i18n/                   # i18next 設定とロケール
    ├── store/converter-store.ts
    ├── conversion/
    │   ├── adapt.ts            # CanonicalValue 間ブリッジ
    │   ├── handlers.ts         # 各タイプの parse/serialize
    │   ├── engine.ts           # 高レベル変換 API
    │   ├── pane-value.ts       # ペイン値型
    │   ├── encoders/           # Base64/32/58/Ascii85/Z85/Crockford
    │   ├── escape/             # URL/Unicode/HTML/XML
    │   ├── structured/         # JSON/YAML/JSON String/JSON Byte/Hex
    │   ├── binary/data-url.ts
    │   ├── qr/                 # QR generator / reader
    │   └── barcode/            # バーコード generator (bwip-js) / reader (@zxing/library)
    └── components/
        ├── TitleBar.tsx
        ├── UpdateNotifier.tsx
        └── converter/          # 変換 UI (左右共通コンポーネント)
            ├── ConverterApp.tsx        # 3 列レイアウト + 上部/下段バー
            ├── FavoritesBar.tsx        # 上部バー: お気に入り (左) + 直近の変換チップ (右)
            ├── Pane.tsx                # 左右共通: side だけ受け取り role を導出
            ├── PaneHeader.tsx          # 役割ラベル + cat/type/search
            ├── PaneSelectors.tsx       # cat/type ドロップダウン + 検索アイコン
            ├── PaneContent.tsx         # display で振り分け (text/binary/image)
            ├── TextPane.tsx            # テキスト系 (入出力共通)
            ├── BinaryPane.tsx          # バイナリ系 (role で in/out 切替)
            ├── ImagePane.tsx           # 画像系 (role で in/out 切替)
            ├── ImagePreview.tsx        # 画像プレビュー (contain)
            ├── QrOptionsPanel.tsx      # QR 生成オプション
            ├── BarcodeOptionsPanel.tsx # バーコード生成オプション (規格選択ほか)
            ├── CenterControls.tsx      # 中央列: 入れ替え (上) + お気に入り登録 (中) + 変換 (下/右矢印)
            ├── BottomBar.tsx           # 下段メッセージバー (MUI Alert / × で閉じる)
            ├── TypeSearchButton.tsx    # カテゴリ横断タイプ検索
            ├── CopyButton.tsx
            └── shared.tsx              # 共通定数 (PREVIEWABLE_MIMES / RAW_MIME / sx / EmptyStateBox 等)
```

## 3. データフロー

### 3.1 変換実行 (左 → 右の場合)

```text
[左ペイン: text | binary | image]
        │  PaneValue
        ▼
parsePane(inputType, value)
        │  CanonicalValue
        ▼
adapt(canonical, outputDomain)
        │  CanonicalValue (target domain)
        ▼
serializePane(outputType, canonical, ctx)
        │  PaneValue
        ▼
[右ペイン: text | binary | image]
```

`ctx.computeHash` / `ctx.detectMime` は IPC 経由でメインプロセスを呼ぶ非同期関数。

### 3.2 ファイル選択 (バイナリ入力)

```text
[Open ボタン] → window.encodelab.file.open() → IPC → main: dialog.showOpenDialog
                                                       fs.readFile
                                              ← Uint8Array (+ name)
[MIME 判定]   → window.encodelab.mime.detect() → IPC → main: file-type
                                              ← { mime, ext }
[Pane 更新]
```

### 3.3 ハッシュ出力 (canonical を経由しない特例)

```text
[入力ペイン] ─inputBytesForHash()→ Uint8Array
                                         ↓
                          ctx.computeHash() ─IPC→ main: crypto
                                         ↓
                                テキスト (hex / base64)
                                         ↓
                                 [出力ペイン]
```

`runConversion` は出力タイプが hash 系の場合、parse/adapt/serialize の経路をスキップして
`inputBytesForHash` でユーザーが見ているそのままのバイト列 (テキストなら UTF-8 エンコード) を取り出し、
直接ハッシュする。これにより JSON / YAML 入力が「再シリアライズ後」の文字列としてハッシュされる
不具合を回避している。

## 4. 状態管理 (Zustand)

`src/renderer/store/converter-store.ts` で以下を保持。

| 項目 | 説明 |
| --- | --- |
| `direction` | 入出力方向 (`'rtl'` = 左入力 / 右出力)。現在は `'rtl'` 固定運用 |
| `left` / `right` | 各ペインの `{ category, type, value }` |
| `qrOptions` | QR 生成オプション |
| `barcodeOptions` | バーコード生成オプション (規格 / 形式 / 倍率 / 高さ / 文字表示 / 色) |
| `favorites` | 登録済みお気に入り `Favorite[]` (入出力タイプの組合せ)。`userData/settings.json` に永続化 |
| `recentConversions` | 直近に変換した入出力パターン `Favorite[]` (新しい順・最大 5 件)。永続化 |
| `message` | 下段バーに表示する変換結果メッセージ `{ severity, text }`。未設定ならバー非表示 |

派生関数:

- `inputSide(direction)` / `outputSide(direction)` — direction から入出力サイドを導出
- `favoriteId(inputType, outputType)` — お気に入りの安定 ID (`入力>出力`) を生成

入れ替え操作:

- `swapPanes()` — 左右ペインの `{ category, type, value }` を物理的に入れ替える。
  中央列の入れ替えボタンから呼ばれ、入力と出力の役割を反転させる。

お気に入り / 直近の変換:

- `toggleFavorite()` — 現在の入出力パターンを登録 / 解除し、永続化する。
- `removeFavorite(id)` / `applyFavorite(id)` — 指定お気に入りの削除 / 左右ペインへの適用 (データは維持)。
- `setFavorites(list)` / `setRecentConversions(list)` — 起動時に永続化済みデータを反映する (永続化はしない)。
- `recordRecentConversion()` — 変換成功時に現在のパターンを直近履歴へ記録する (永続化する)。
- `applyPattern(pattern)` — `Favorite` 形のパターンを左右ペインへ適用する (履歴チップから利用)。

## 5. 拡張時のガイドライン

### 5.1 新規タイプを追加する

1. `src/shared/conversion/catalog.ts` の `CATEGORIES` に `TypeDefinition` を追加。`id` は重複しない値にする。
2. `src/renderer/i18n/locales/{ja,en}.ts` の `type.*` にラベルを追加。
3. `src/renderer/conversion/handlers.ts` の `parseTextType` / `serializeTextType` (display=`text` の場合) に分岐を追加。display が `binary` / `image` のものは `parsePane` / `serializePane` 本体を編集。
4. 必要であれば独自エンコーダを `src/renderer/conversion/{encoders,escape,structured,binary,qr}/` 配下に追加。
5. 入出力に意味的な制約があれば `src/shared/conversion/compatibility.ts` を更新。

### 5.2 ハッシュアルゴリズムを追加する

1. `src/shared/types.ts` の `HashAlgorithm` に値を追加。
2. `src/main/services/hash.ts` の `NODE_ALGO` にマッピングを追加。
3. catalog にタイプ追加 + `src/renderer/conversion/handlers.ts` の `computeHashOf` に分岐を追加。
   ハッシュ出力は `engine.runConversion` 側で canonical 経路をスキップし、`computeHashOf` を直接呼ぶ。

### 5.3 メイン側 API を追加する

1. `src/shared/constants.ts` にチャンネル名を追加。
2. `src/main/ipc/*.ts` に handler を実装し `registerIpcHandlers` から呼ぶ。
3. `src/preload/index.ts` の `IPC_CHANNELS` と `api` に追加。
4. `src/shared/ipc.ts` の `IpcApi` 型を更新。
