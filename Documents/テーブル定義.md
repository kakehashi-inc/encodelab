# テーブル定義 — EncodeLab

最終更新: 2026-04-29

EncodeLab は変換ツールであり、現バージョンでは永続化対象データを持たない。

- 変換履歴は保持しない (要件 §11)
- アプリ設定 (テーマ / 言語) は OS 設定および `nativeTheme` から取得し、保存していない
- データベース (SQLite 等) は未利用

将来的に履歴・お気に入り・カスタムプリセットなどを永続化する場合は、本ファイルに各テーブル単位で構造を追記する。
